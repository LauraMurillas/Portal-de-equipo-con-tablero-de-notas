{
  "name": "portal-equipo-notas",
  "private": true,
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web && npm run build -w lambda/metrics",
    "test": "npm run test -w apps/api",
    "db:generate": "npm run prisma:generate -w apps/api",
    "db:push": "npm run prisma:push -w apps/api",
    "db:seed": "npm run prisma:seed -w apps/api"
  },
  "workspaces": ["apps/*", "packages/*", "lambda/*"],
  "devDependencies": { "concurrently": "^9.1.2" }
}
